"""Streamlit entrypoint wrapper for Streamlit Community Cloud.
This file imports `ui.py` which contains the Streamlit app.
"""
import ui

# `ui.py` runs on import because it defines Streamlit UI at module level.
